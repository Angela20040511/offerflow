<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.offerflow.mapper.GuideDocMapper">

    <resultMap id="GuideDocResultMap" type="com.offerflow.entity.GuideDoc">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="title" column="title"/>
        <result property="fileUrl" column="file_url"/>
        <result property="sortNum" column="sort_num"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAllOrderBySort" resultMap="GuideDocResultMap">
        SELECT
            id,
            code,
            title,
            file_url,
            sort_num,
            create_time,
            update_time
        FROM guide_doc
        ORDER BY sort_num ASC, id ASC
    </select>

    <select id="selectByCode" resultMap="GuideDocResultMap">
        SELECT
            id,
            code,
            title,
            file_url,
            sort_num,
            create_time,
            update_time
        FROM guide_doc
        WHERE code = #{code}
        LIMIT 1
    </select>

</mapper>